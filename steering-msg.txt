【编排者中途纪律修正，优先级高于原任务描述中的对应表述】

1. 禁止把游戏方言当引擎语义。survey 里「说话人以 ;//【名字】 注释标注」是 SWAN SONG 汉化组的标注习惯，不是 KAG 引擎机制——注释一律按注释处理（忽略）。说话人归属必须从真实引擎语义推导：去查宏层与 system/KAGLayer.tjs、MessageLayer.tjs 里 KAG 的名字显示机制（如 [name] 类标签、name 属性、或本作实际用头像 kao 表达说话人）。若本作实际不显示文字名（靠头像区分），转写产物就如实不带说话人名，并在报告中说明依据。

2. 宏层必须完整实现：[macro]/[endmacro] 定义、参数传递（%key 与默认值）、宏体内允许的全部语法（标签、文本、控制流、[er]、嵌套调用）都要按 KAG 语义完整实现。语料普查（103 定义/41 调用）只用于确定覆盖面与测试集，不许只实现「剧情用到的那 41 个」的特化展开。

3. 总体原则：Krkr 解析器是对 KAG/Kirikiri 引擎本身的完整复刻——标签语法、行形态、属性规则、TJS 嵌入（[eval]/[embexp]/[iscript]）以引擎语义为准；单个游戏的习惯、巧合、残留只允许出现在「覆盖面决策」和「测试语料」里，绝不允许出现在解析规则里。发现 survey 中其他把方言当语义的地方，一并按此原则纠正并在报告中列出。

其余任务目标、验收标准、文件边界不变。
